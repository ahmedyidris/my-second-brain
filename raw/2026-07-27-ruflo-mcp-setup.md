# Ruflo — MCP Server in Claude Code

Canonical form (matches USERGUIDE.md):

```bash
claude mcp add ruflo -- npx ruflo@latest mcp start
```

Adds Ruflo as an MCP server in Claude Code so it's available as a tool.
