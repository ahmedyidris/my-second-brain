# Composio — Setup

## Install
```bash
curl -fsSL https://composio.dev/install | bash
```

## Login
```bash
composio login
```

Composio is a tool connector with 1000+ app integrations. Used in [[the-council]] as the "Tool Connector" member.
Previously configured in the DeepSeek master plan via web UI (Customize → Connectors → Add Custom Connector, URL: https://connect.composio.dev/mcp).
